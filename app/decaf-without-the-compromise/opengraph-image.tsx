import OpengraphImage from "components/opengraph-image";

export default async function Image() {
  return await OpengraphImage({
    title: "Decaf Without the Compromise",
    description: "Real coffee taste. No caffeine.",
  });
}
